# F18：PC 結果、全站風格與請求切換

2026-09-12，Frankie 整合。基線 main 0e20c6e。

## 產品行為

- 結果摘要先呈現 PC1／PC2／PC3，逐 PC 合併保存條件的狀態與說明；共用前提另列。報告共用相同統整函式。
- 核心的八項條件為四項共用前提（build 身分、library 綁定、產品綁定、範圍完整），PC1 元件一項，PC2 脆弱實作一項，PC3 入口與必要條件兩項。五 Query 是取證工作，與條件數不同。
- 僅核心整體 AFFECTED 且該 PC 條件均 SUPPORTED 時，PC 支持條件使用深紅字／淡紅底。共用前提保持中性色；UNKNOWN 使用琥珀色。不是另算 PC verdict，也不是感染／利用證明。
- 保存資料仍以 literal text 顯示；CSS 完全靜態。拒絕缺項或重疊的分組，保留原始條件明細。
- 側邊深藍綠底、淺色展開面板、深色文字，展開時另有底色與分隔線；主頁同一組展開／收合樣式，主操作綠色、焦點藍框。顏色另搭文字狀態，不只靠顏色。
- 請求選單增加資料包與情境摘要。開啟 CVE 沿明確 parent 鏈選最新紀錄，保留失敗和補件，不再回到最初收件。不能以相同 CVE 掛接另一請求；主頁與選單不同時明示。

## 本機驗收

- CMake 04／05／06 真包重新收件及 Q1–Q5：AFFECTED／NOT_AFFECTED／NEEDS_INVESTIGATION；三組 PC 映射 4＋1＋1＋2，報告與補件出口 AppTest 通過，無 API 呼叫。
- 04 受影響 run：1aa4adba-0b2d-4f74-83a2-0c11c208c0ac；archive SHA256 b9fb05eef063b53d85c29ebcb5076963cf7927a04ebeafdf1cf0b940bdaba5a0。
- 多 CVE 請求 5ad87730-6dab-4fa1-a1ee-5cf24b3ff8f8：真 04 → 工程結果 → AI 未執行／報告 → 未支援 CVE → 報告 → 切回 04。回到原工程 8a3f45b8-9b83-4dc1-b2c4-eca3c8c33938，未新增分析、未混用另一 CVE 報告、零 LIVE 呼叫。
- 本批初次完整回歸 189 passed、23 subtests，43.82 秒；schema 無差異。後續整合固定 head 以發布文件／CI 為準。

## 主 Demo（兩條，不把測試腳本放進網站）

1. 確認受影響：04_cmake，CVE-2022-37434。從請求切換開啟，展示 PC1／2／3、原文與報告。說法為「工程證據支持受影響」，不能說已感染。
2. 需要補件：06_cmake，相同 CVE。展示 NEEDS_INVESTIGATION、缺少綁定與程式路徑；使用 04 同 build 對 06 的正式 delta，另存新 run、重新分析，再比較前後。原本缺件的工程 run 保留。

若先前提出未解決的其他入口／範圍聲明，補入一般 delta 不保證轉 Affected。必須保留歷史讓核心重新判定；不可為了 Demo 抹除聲明。

## 發布與限制

維持既有 Google allowlist、使用者隔離、8507 與私人 API 配置。發布前完整回歸／schema／CI，無活躍 worker 才更新固定版本；保留回復版本及 runtime。排程仍是每五分鐘巡檢，不保證電腦離線時即時更新。

AI 邊界沿用 D01／D04／D06／D08／D09、R01／R06／R07／R11／R12。此次不新增 LIVE、不改核心漏洞規則、不宣稱沒有任何可能的 bug；驗收覆蓋指定流程。
