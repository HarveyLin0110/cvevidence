# 帳號工作台的輸入邊界

2026-09-12。workspace 使用明確 store_root 的帳號模式時，只提供共享產品樣品、上傳工程包、情境輸入；本機操作者模式保留受控相對路徑。

這個變更避免多人網站提供伺服器路徑輸入介面，不取代下層路徑檢查、上傳驗證或帳號 RunStore 隔離。共享產品樣品仍由可信任 catalog 提供；用戶本機的路徑不是伺服器可直接讀取的位置。

測試：帳號模式的 Streamlit AppTest 確認沒有受控路徑且保留上傳選項，既有真實 CMake 操作與登入身分測試同時通過。對應 D05、R01、R03；沒有宣稱完整沙箱。
