# F14d 五步導覽

2026-09-12。workflow_navigation.sidebar_steps 由呼叫端傳入已核對的 has_run／failed／has_engineering，提供五個側邊按鈕與反灰條件。不自行讀保存欄位、不啟動分析。

未有run只能輸入；收件後可確認、執行工程及匯出收件紀錄；保存工程結果後開啟AI頁；失敗可返回輸入或查看失敗紀錄。舊04報告導覽相容到05；不可用的舊選取自動回可達頁面。

83 pytest passed；新增導覽狀態與callback／失效選取復原兩項測試。工作台接線在正式70b3b68工程介面交付後進行。
