# 評分項目與可驗收證據

依主辦 TW Briefing 第 5–8 頁整理。評分重點包含六項，原簡報未列權重；不自行編造配分。此表供 HV 準備報告，僅把已實測成果寫成完成。

## 評分與展示證據

| 主辦評分項目 | 今天應展示的內容 | 驗收證據 | 現況 |
|---|---|---|---|
| 問題與解決方案適切性 | 工程師只知道異常，或收到 CVE 通知；兩種入口都能接續到工程查核 | 無檔案收件、指定 CVE、補件往返的實際操作 | 核心兩入口與補件已驗；UI 待 Frankie 完整接線 |
| 開發品質 | 真實 build、可追查引用、拒收篡改與混版、重啟仍可操作 | build/測試 log、反例測試、九格與重啟紀錄 | 六個選用 build、九格、補件通過；31 邊界測試與 30 次 OFFLINE 穩定性 |
| 洞察與創新性 | 相同版本仍可能因編譯/修補而不同；AI 會隨缺口追加新的問題 | ROM 同版本 on/off、curl 同版本官方 patch；資料變體下不同工具選擇 | 同版本對照已驗；四個 Live 情境有不同查詢／補件行動 |
| 實際應用價值 | 使用者得到可交給下一位工程師的證據、理由與具體補件工作 | 下載摘要、原文定位、同 build 補件前後比較；記錄真實耗時 | 中文摘要／補件要求與原文已提供；未量測節省人時 |
| 開發方向契合度 | 深度領域 AI + 自主且具適應能力的 AI | CVE 特有條件、AI 真實工具呼叫、口述與原檔分級、未知條件保留 | 三個 CVE profile、Q1–Q5 與四情境 Live 已驗；引用／hash 仍可拒絕模型錯誤 |
| Codex 應用深度 | Codex 寫新程式、執行真 build、從失敗 log 修正並驗證 | 今日 commit/變更、命令與失敗/修正/成功紀錄 | curl libtool 漏 header 取證 → 修 compiler observer → 重建兩版 → 真實綁定与九格通過 |

主辦明示開發品質著重產品、程式與可靠性。因此先取得真實可重現證據，再把它整理成簡報；不以漂亮畫面替代未完成能力。

## 六分鐘可用的重點順序

1. 收到 CVE 通知但供應商只說已停用：ROM 03 先查，再補回 02 同次建置原檔，條件充分才轉 Not Affected。
2. 展開 AI 新增的 library 對應問題與實際唯讀工具紀錄，讓評審能打開引用。
3. CMake 從 gzip 截短讀取錯誤開始，找有來源的 zlib 候選；清楚說明截短錯誤並非 CVE 已觸發。
4. 用 curl 修補/使用條件及九格實測表補充適用範圍，下載可追查摘要。
5. 展示一筆今日 Codex「失敗 → 根據紀錄修正 → 重測」證據。

## 成績表必須分開計數

- 六個新 build：只有完成命令、產物 hash 及正常功能觀測才計數。
- 九包資料：manifest、archive、hash、同 build 補件關係驗過才計數。
- 九格工程判定：實際 collect/verify/assess 跑完，對照獨立驗收預期才計數。
- 三條補件：保留原 run，核對同一成品，新增快照重算才計數。
- Live AI：至少三次實際 OpenAI 呼叫與工具/補件紀錄，另外記失敗、耗時與模式。Offline 或 Replay 不計為 Live。

## 有助展示的優化及狀態

| 優化 | 為什麼有幫助 | 狀態 |
|---|---|---|
| 每項正式結論能展開到原文 | 評審可直接核對，不依賴口頭保證 | E-ID／X-ID 与來源 witnesses 可展開；前端由 Frankie 呈現 |
| 相同版本、不同 build 的對照 | 說清楚版本掃描之後仍需要工程證據 | ROM on/off、curl 官方 backport 同版本差異已驗 |
| 原檔提前補齊/改包名變體 | 驗證 AI 沒有照固定劇本，規則不依標籤 | 改名／時間不變結果通過；curl 已提供材料時 READ 後完成，缺件時 ASK_USER |
| 各自一份短同步 Markdown | 避免兩位開發者重寫 AI/規則、縮短接線往返 | 使用者已選定；Horace.md 已上 Git |
| 每輪 AI 工具預算與停止理由可見 | 說明代理程式可控制、失敗時仍有工程結果 | 8 次工具、90 秒預算與停止原因已實作；真實假 X-ID 被拒並保留 |

## 提交前仍須確認

主辦要求 17:35 前提交新公開 GitHub repo、專案說明、特殊使用 OSS/當日新增內容；Demo 連結選填。repo 目前為 private，公開及正式提交屬 Frankie/HV 的整合提交工作，尚未完成。Horace 提供來源、授權、可執行核心與可核對成績。

唯一當日產出根目錄：`CVEvidence_Fresh_2026-09-12`。舊 `CodexHackathon` 只供概念參考，不將舊成品混入完成數。

完整交件：[Horace 核心與 Demo](../releases/Horace_完整核心與Demo交件.md)。可交評審核對的輸入在 Git 的 `demo-inputs/`，真實結果在 `docs/releases/驗收證據/`。另有 Live 上傳文字注入測試，模型使用 LIST/READ/COMPLETE，沒有執行要求的 shell；工程結果保持 Affected，Replay 明確標為 REPLAY。
