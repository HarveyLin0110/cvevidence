# 最小補件卡與調查收尾一致性

程式 f19d748。通用 CVE 調查已 REVIEW 且仍有 USER_MATERIAL_MISSING、沒有 OBSERVED_EXCLUSION／CONFLICT／CAPABILITY_GAP 時，COMPLETE 不能只在摘要索取材料；核心要求用 ASK_USER，經原本最小項目、單條件與既有材料檢查後產生補件卡。沒有放寬引用、狀態或補件門檻。工具缺口／衝突／排除線索仍可收尾交人工覆核，不強迫補件；條件狀態語意仍是待覆核 AI 提案。

## 真實驗收

同一新編譯 TEST_ONLY ELF／原碼、同 run 63976040-37e0-4080-be9c-fc0c7a7a64d6、CVE-2023-38546；沒有公司材料或新增供應者。

- OpenAI API，gpt-5.6-sol／high，AI db215ee0-652c-4fec-bdb8-30b42d72ec49。
- 117.73 秒、8 calls、170625 tokens（供應者回報，非費用）。NEEDS_USER_INPUT，成功保存一項結構化補件，原工程 run bytes 不變。
- 僅要求一份既有建置紀錄，說明向建置／CI 工程師取得、需要輸入清單／輸出身分，以核對 source-to-ELF；不同既有紀錄為擇一方案，不要求重新建置其他樣品或 PC3 全套資料。
- 實際 8506 網頁顯示最新 LIVE、NEEDS_USER_INPUT、單一「本輪最小補件」卡、取得方式與詳細展開。

仍有三個 TOOL_ERROR：第一次 PLAN 的公告逐字引用不符；第一次 ASK_USER 使用搜尋詞命中已收 ELF 但未有文字 READ（解析 metadata 不等同文字原文）；一次 COMPLETE 被新規則拒絕，最後 ASK_USER 通過。這是可恢復流程通過，不是零重試或高效率驗收。下一輪需優先減少引用及精確檔名搜尋造成的重試、重複輸入；也仍未真正提交補件與完成成品綁定。

新增 mock 流程確認 prose-only COMPLETE 被擋後可改為有效 ASK_USER；保留工具限制／排除／衝突交覆核的路徑。mock 不代表真實模型品質。D01/D02/D05–09；R01/R05–09/R11/R12。只推個人分支。

完整本機嚴格原生 ROM 回歸：**527 passed、23 subtests passed**（291.70 秒），一項既有 ZIP 警告。程式 CI 36204923946 提交時仍在執行，不能列為已通過。
