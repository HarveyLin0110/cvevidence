# 混合產品材料收件收尾

使用者希望本輪集中完成已知問題，暫停新增問題探索。

## 已完成

首頁統一「上傳產品材料」入口，ROM、SBOM、SDK／原碼、設定可以屬於同案件。下一層依容量選一般多檔或大型單檔；保留原先單檔／總量／檔案數限制，並指出先交大型檔、再於「05 報告與後續行動」補其他材料的路徑。不是開放無限制多個大檔，也不自動認證材料同建置。

原本封存檔解開後只保存其中 ROM，未呼叫韌體盤點。本輪將直接上傳與一層解開的 ROM 一起送往既有隔離、唯讀解析，合計仍最多三份、共用容量上限。內層 ZIP／tar 保留為資料、不遞迴。封存檔不允許自行攜入工具產生的 .rom-inventory 收據路徑，避免混淆來源。

## 驗收

聚焦 32 passed（6.59 秒）。新增四項：真實新製 TEST_ONLY SquashFS＋原碼＋清單同包、直接／內包 ROM 共用數量限制、拒絕偽裝盤點路徑、內層封存檔保持不透明。真實 SquashFS 能取得 curl 8.3.0-vendor2 宣告；這是解析驗收，不是產品影響或同建置認證。更新 UI 測試確認兩種容量模式與補件說明。

已重啟 8506，實際瀏覽器切換「上傳產品材料」及兩種容量模式，核對 20／256／384 MiB、混合材料與補件說明。未驗收原生選檔 HTTP 上傳，亦沒有新增模型 API 呼叫。完整回歸 669 passed、23 subtests passed（265.32 秒），既有重複 ZIP 名稱警告一項。核心 e2e4beb、入口 9607aff；前一輪閱讀容量 2cb4a05。

D01／D02／D06–D09：範圍收斂、資料不進 Git、分開提交、測試與限制。R01／R03／R05–R07／R09／R11：既有 scope／hash、隔離唯讀、未知不推成判定、資源上限與不可變補件不變。保留 Harvey 的 Runner／儲存及正式判定方向。

本輪交付後暫停主動擴大開發；未完成的公司部署、可信建置與其他格式／跨案例品質留待下次排程，不宣稱整個公司上線目標已完成。
