# 公告引用修正與重複調查輸入

程式提交：ddbdd48（引文修正提示）、606aa79（精簡重複回覆）。

- 公告引文不符時指出 condition ID／source ID，從同一已取得來源提供最多三段、每段 600 字元的原文及字元位置。僅做有限字詞比對，不推論語意、不跨來源替換、不自動接受條件。原引文仍被拒絕，模型須重新選擇並通過逐字檢查。
- PLAN／REVIEW 成功後，模型收到接受回覆、plan hash、各條件狀態與提醒，不再收到剛提交的完整條件重複副本。完整條件仍保存在 condition_plan 與 task.result，實際精簡回覆保存在 task.model_feedback；兩者可追查。
- 通用 legacy API 路徑與共用 provider history 都使用相同回覆。沒有改正式判定、引用核對或補件限制。

## 真實同材料驗收

工程 run 63976040-37e0-4080-be9c-fc0c7a7a64d6，TEST_ONLY 新編譯 ELF 與原碼，CVE-2023-38546。OpenAI API、gpt-5.6-sol／high。

| 紀錄 | 呼叫 | 秒數 | 回報 tokens | 結果 |
| --- | ---: | ---: | ---: | --- |
| 前次 db215ee0-652c-4fec-bdb8-30b42d72ec49 | 8 | 117.73 | 170625 | 三次工具拒絕後，單項補件成功 |
| 本次 8f8135bc-a654-4861-aa1c-150de0ece81c | 5 | 70.85 | 81448 | PLAN→READ→READ→REVIEW→ASK_USER 全部成功 |

本次仍只要求一份原始建置紀錄，說明取得角色、輸入／輸出對應與替代 attestation；原工程 bytes 不變。實際 8506 網頁核對最新 LIVE／NEEDS_USER_INPUT、81448 tokens 及單項補件卡。

完整 PLAN／REVIEW result 的 JSON（預設 ensure_ascii）各 8170／9428 字元，送模型的回覆各 567／621 字元。縮小回覆是可重現的程式效果；整輪 tokens／時間是單次真實觀察，包含模型行為變動，不代表固定節省比例或跨案例品質保證。本次沒有觸發錯誤引文修正提示；該提示由單元與模擬流程驗證，不能宣稱此次 LIVE 證明它成功修正模型。

D01/D02/D05–09；R01/R02/R05–09/R11/R12。尚未完成真實補件、成品綁定、公司 OIDC／內網與多案例人工品質驗收；只推個人分支。

本機嚴格原生 ROM 完整回歸：**530 passed、23 subtests passed**（273.33 秒），一項既有 ZIP 警告；引用／通用調查／provider 核心聚焦驗證 50 項通過。程式 CI 36205478451 紀錄時仍在執行。
