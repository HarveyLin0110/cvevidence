# 團隊站 ROM 工程展示實測

驗收時間：2026-09-12 14:49–14:56（Asia/Taipei）。Horace 使用真實 Chrome、已登入帳號及產品介面操作；未代寫網站結果或讀取服務端 runtime。

入口：[團隊測試站](https://laptop-5tcfdp5e.tailea98bd.ts.net/)。頁面顯示部署版本 **`ea5e571a1b80a16d8fd5e50366a93fc693a1cb62`**。這是本次實測版本，不等同 Horace PR #9 最新核心；本輪沒有部署或重啟服務。

## 實測步驟與結果

1. 第 01 步選 `fresh-rom · 03_rom · fresh-rom-r2`，指定 `CVE-2014-0160`。情境：「掃描報告指出設備韌體包含 OpenSSL 1.0.1f，請確認 CVE-2014-0160 是否適用本次交付的成品。目前只有 ROM 與部分 SDK，請以本次成品與 build 為範圍查核，並列出需要補齊的原始材料。」
2. 第 02 步確認 432 個來源、build `rom-20260912T043812-0b9b1a`；第 03 步執行 Q1–Q5。初判 **Needs Investigation**：2 支持、0 阻斷、6 未知。Q1、Q3、Q4 有缺件，Q2、Q5 COMPLETED；query 完成不代表所有衍生條件成立。
3. 第 04 步實際為 **OFFLINE／OFFLINE**，並明示「LIVE 調查入口尚未接線」。初判報告預覽有條件、缺件、E-ID 與 witnesses；點下載後收到瀏覽器下載事件。
4. 選 `supplement_03_rom · fresh-rom-r2`，按「套用已取得的補件並建立新 run」，回第 03 步重新分析。
5. 補件後 **Not Affected（限本次成品與 CVE）**：7 支持、1 阻斷、0 未知；來源增至 4,294。同 build 與原 ROM hash 保留；脆弱實作條件從 UNKNOWN 轉 BLOCKED。
6. 第 05 步顯示原工程 run、不同 parent/child context、六項條件差異及前後結論。補件後完整報告含正確 run、結論及原 ROM hash。
7. 重新載入網頁，從「查核紀錄與歷程」載入原初判。原報告完整 DOM code 文字與補件前完全相同，仍 Needs Investigation，未混入補件後 run；四筆收件／工程紀錄仍在歷史中。

## 識別資訊

| 項目 | 值 |
|---|---|
| Request | `3b6d967d-f751-42ef-8598-33565679da8f` |
| 初始收件 run 前綴 | `5b91c389` |
| 原工程 run | `2181d491-8ecd-470c-9ff4-65436797c2dc` |
| 補件收件 run | `0ac19b73-2570-4e5d-a928-5391de7a4e3e` |
| 補件後工程 run | `c4b39a19-0982-42db-b4d1-807cbd061c6a` |
| 原 context | `362de586a0b4155ecfb281efd23d1571cfc6e4156833f910dc1ada01fa0d1726` |
| 新 context | `827efba7062537d66050982d6f1bd6aef3c918e93aabf58992e71db3c3c7b971` |
| 同一 ROM SHA-256 | `9ee6e6d88ca2796266798e62130b4c30d48bdb7101df3f4a9115966577582d92` |

紀錄在本次帳號的歷史中；其他帳號可用相同 Git 樣品建立自己的案件。完整畫面文字及報告預覽留在本機忽略的 `var/validation/team-site-rom-ea5e571/`。

## 未完成與限制

- **完整網站 Live AI 未完成**；本輪沒有網站模型呼叫。Horace 本機核心的真 API 驗收另有紀錄。本機 `.env.local` 不進 Git，也不會自動設定網站主機。
- 補件後報告預覽已核對，但第二次下載等 15 秒未收到事件；不宣稱檔案已落到下載資料夾。瀏覽器工具禁止檢視內建下載頁，未繞過限制。
- 本次驗的是網頁重新載入與歷史取回；沒有重啟服務、還原備份或測跨帳號隔離。使用站內今日樣品，不擴稱所有手動上傳途徑通過。
- `ea5e571` 的畫面顯示 witnesses；`analysis_report.py` 第 42 行只匯出 evidence_id/value/reason/witnesses，尚未直接附核心 `evidence.excerpts` 的 X-ID、行號與原文。來源檢視入口仍在，原文點選及匯出須另驗。

## 交接

整合端取得 PR #9 修正後，在網站主機的可信程序設定 `OPENAI_API_KEY`、`OPENAI_MODEL=gpt-5.6-sol`、`OPENAI_REASONING_EFFORT=medium`，呼叫 `investigate_after_engineering` 並獨立保存 AI stage。只填金鑰不會使目前 OFFLINE 按鈕自動變成 Live。

接線依 [核心介面文件](../architecture/核心分析介面與接線提案.md)。本次正式站工程補件流程已有實測；完整 Live、原文呈現、補件後下載及服務重啟仍分項完成。
