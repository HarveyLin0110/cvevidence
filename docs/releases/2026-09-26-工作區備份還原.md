# 工作區備份還原

程式 3cb6e47：新增唯一本機工作區的 create／verify／restore 指令。逐檔 SHA256、分段搬移、允許的資料路徑、容量限制、來源變動檢查、拒絕覆寫，還原先驗證到暫存資料夾再發布。保留未完成請求 fence，不恢复背景工作；不外送資料、不改判定、金鑰設定不納入。

12 項測試通過（2.01 秒）：真實 Runner 請求／工程包還原、排除操作者設定與暫存、拒絕覆寫、路徑穿越、未列出內容、blob hash 不符、來源 symlink／變動、容量、ZIP symlink 及未完成請求 lock 保留。

實際演練從已保存的 TEST_ONLY ROM／SDK 案例複製兩個工程 run、AI start/end 收據與三份 blob 到獨立測試 store，非備份整個使用者工作區。CLI create→verify→restore 均成功，7 檔／189621 bytes；工程 JSON 及 AIStore 讀取結果相同，還原後重新讀取原碼證據成功。

工程 run 9083f4e1-5301-4b8c-b203-85b00928bbb1、AI f8efe6fa-b169-42d9-a1d0-dadbbde931ca。以 localhost:8507 臨時入口讀取還原工作區，實際網頁確認原 LIVE 收據、125749 tokens、PC2 解釋與 linker map 單項補件卡仍存在；沒有重新呼叫模型。驗證後已停止臨時服務並回到 8506，未切換現行工作區。

嚴格原生 ROM 完整回歸 596 passed、23 subtests passed（287.11 秒），一項既有 ZIP 警告。CI 36209995799 尚在執行；上輪 CI 36209543897 成功。操作與限制見 [工作區備份與還原](../operations/工作區備份與還原.md)。未加密、非來源認證、未配置公司備份排程或異地政策；只推個人分支，Harvey 決定合併。
